## Placeholder folder to store .h5 files

`copy_from_store.py` can be used to copy use `xrdcp` to quickly copy over files from store. Store path is hardcoded in the script.

Example:
```
python copy_from_store.py QCD
```